# Kickoff note from the L & N session, 2026-09-22

You are a fork of the L & N session, so you already carry the store-publishing experience: the CDP/noVNC browser on display :165 (CDP 9485), the App Store Connect API helper, the Mac build host, the Play Console helpers, the paid/IAP lesson, and the `MARKETING_VERSION` lesson that cost 1.0.5 an Invalid Binary.

Your folder is `/home/lachlan/ProjectsLFS/ZhJpBook` (the PocketPolyglot repo). Read `app/BRIEF.md` first, then `AGENTS.md` and `NOTICE.md`.

**The Codex session that prepared these books** is `019e1f99-289e-7711-986a-d41047f5ed21`, rollout `~/.codex/sessions/2026/05/13/rollout-2026-05-13T12-29-51-019e1f99-289e-7711-986a-d41047f5ed21.jsonl`, 2.6 GB, still owned by a live process. Read it, never write it, and never kill its owner. Use the `codex-collaboration` skill: `python3 ~/.claude/skills/codex-collaboration/scripts/codex_session.py tail 019e1f99 -n 40 --all`, then `extract ... --out ~/.codex/handoffs/zhjpbook-extract --transcript` and hand ranges to subagents. Its last work (2026-08-28) was the colour editorial vocabulary dictionaries; the interlinear book pipeline is earlier in the thread.

**Shared resources.** Two other sessions are live: LazyOracle (its own tabs on CDP 9485, Play app 4975413590951100628, ASC app 6814663937, Mac `~/Projects/LazyOracle`) and L & N (its own tabs, Play apps 4973965106419739455 and 4976381008764842239, ASC app 6808872450, Mac `~/Projects/L-And-N`, plus a private headless Chrome on CDP 9486). Create your own tabs, record their ids in your scratchpad, touch nobody else's, and message before restarting the :165/9485 stack. Both sessions are reachable with SendMessage.

**Not yours to change:** `../L-And-N`, `../LazyOracle`, and the existing TeX/Python pipeline in this repo beyond what the app needs. Add the app under `app/`.

Start with milestone 1: the rights audit and the bundle builder. The owner wants the app finished and formally submitted for review without further involvement, so keep going through the store submission, and commit and push as you go.
